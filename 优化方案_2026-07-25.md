# video-analysis-gemini 优化方案（基于 TOP01 首跑验收）

> 日期：2026-07-25 ｜ 依据：`验收报告_TOP01_2026-07-25.md` + 脚本源码走读
> 原则：每条改进都定位到具体文件与行号，附验证方法；分三批执行，批次 1 是批量跑的前置条件。

---

## 批次 1 · 阻断项（跑 TOP02 之前必须完成）

### 1.1 多轮质检留痕（P1-1）

**问题**：首轮 Pro 结果与 QA 结果被第二轮覆盖；修补发生在组包之间、未经 `--ingest-pro-result`，`_state.json` 无 `PROBE_REPAIRING` 记录。熔断机制的"3 轮"无审计轨迹支撑。

**根因**（`pro_qa_inspector.py`）：
- L541-548：`_qa_result.json` 单文件直写，每轮覆盖
- L647-655：阶段A 组包只追加 `PRO_AUDITING`，不感知 analysis 是否在两轮之间被修改
- L437：`retry_count` 靠数 history 里的 `PROBE_REPAIRING`，手动修补路径永远数不到

**改法**：
1. **轮次归档**：ingest 时先将当轮 `_pro_review_result.json` 复制为 `_pro_review_result_r<N>.json`、`_qa_result.json` 复制为 `_qa_result_r<N>.json`（N = history 中 PRO_AUDITING 次数），再写主文件。主文件永远是"当前轮"，r<N> 文件是历史。
2. **修补自动感知**：阶段A 组包时计算 analysis 文件 md5 写入 packet 与 state；若发现已存在旧 packet 且 md5 变了 → 自动追加 `PROBE_REPAIRING` 到 history 再组包。这样"手动 patch + 重新组包"的路径也会留下熔断计数，不强制改工作习惯。
3. **ingest 一致性校验**：ingest 时比对 packet 里的 md5 与当前 analysis md5，不一致则拒绝回收并提示重新组包——防止拿旧包审新数据。

### 1.2 归档账簿建立（P1-2）

**问题**：`_account_meta.json` / `_scan_log.json` 缺失（单视频流程绕过 scan_helper）；`_index.json` 全skill无任何脚本写入，但 `preprocessor.py` L116 的 baseline 注入要读它——断链。

**改法**：
1. `scan_helper.py` 增加 `--init-account` 轻量模式：不扫视频，只建 `_account_meta.json` + `_scan_log.json` + upsert `_index.json`（platform/video_count/analyzed_count）。单视频流程的任务书模板把这一步列为 Layer 1 前置。
2. `ip_sop_compiler.py` 交付成功后自动 upsert `_index.json`：`analyzed_count +1`、回写 `asl_mean`、`formula_name`。闭环后下一条视频的 baseline 注入就是真数据，不再是 `unknown/0`。
3. `preprocessor.py` L200：baseline 读到 unknown 时打印警告（非阻断），提醒先跑 `--init-account`。

---

## 批次 2 · 数据质量（schema 三方契约对齐）

**系统性根因**：prompts.json（生产契约）、validate_schema.py `REQUIRED_FIELDS`（门禁契约）、synthesis_engine/ip_sop_compiler（消费契约）三方各自维护字段名，已产生 3 处漂移。单点修不如收拢。

### 2.1 契约收拢（结构性修复）

- 新建 `scripts/schema_contract.json` 作为唯一事实源：每板块的 top/macro/timeline_item 必填与可选字段
- `validate_schema.py` L28 `REQUIRED_FIELDS`、`unified_gate.py`、`pro_qa_inspector.py` 改为读取该文件
- prompts.json 不动（提示词自然语言），但在 SKILL.md 加注：改 prompts.json 字段必须同步 schema_contract.json

### 2.2 三处已知漂移修复

| 漂移 | 现状 | 修法 |
|---|---|---|
| `hook_analysis` | prompts.json 要求 → Flash 漏填 → 门禁不查（REQUIRED_FIELDS 无）→ formula「Hook 类型: []」 | 契约文件列为必填（软告警）；synthesis_engine 增加兜底：从 `story_beats[beat_type=hook]` 派生 hook_type |
| `silence_moments` vs `ducking_and_silence` | synthesis_engine L122 读前者，prompts.json/实际产出是后者 → formula「停顿数 0.0」恒为 0 | synthesis_engine 改读 `ducking_and_silence`（兼容旧键） |
| `prompt_card` | SOP 可复用模块「提示卡」列空 | 查 prompts.json sop 板块是否声明；未声明则补声明 + 契约必填；编译器加兜底：取模块 description 前 60 字 |

### 2.3 SOP 切频 N/A（P2-2）

- `ip_sop_compiler.py` L100：读不存在的 `cut_frequency_per_min` → 改为内联计算 `total_shots / shots[-1].end_sec * 60`（次/分），并在 SKILL.md 明确单位口径（formula 用镜/秒、SOP 用次/分，二者注明换算）

### 2.4 九宫格退化（P2-1）

**根因**（`preprocessor.py` L100-103）：`fps=1/9` 是"每 9 秒 1 帧"，29s 视频只出 3-4 帧，tile 3x3 其余黑底。注释意图是"每 1/9 时长 1 帧"。

**改法**：先取 duration，`fps={9/duration}`（或 select 等距取 9 点），保证 9 帧填满；同时把"ASL<1.5s 才生成"的文档约定删掉——首跑证明它对 Pro 抽查有用，改为始终生成但生成正确。

---

## 批次 3 · 外观与文档（随手修）

| # | 项 | 位置 | 修法 |
|---|---|---|---|
| 3.1 | formula 泄漏 Python tuple repr（6 处） | synthesis_engine.py L241-243, 250-251, 256-257, 263-264 | 加 `fmt_counter()` 助手，照 L222-227 已有格式统一 |
| 3.2 | ASL 2.66 vs 2.7 口径 | SKILL.md 自检规则 vs 实际输出 | 保留 2 位小数精度，改 SKILL.md 自检文字为"2 位小数"；formula 展示层维持 1 位 |
| 3.3 | L4 重跑重复记 `PASS_DELIVERED` | ip_sop_compiler.py L333-338 | current_state 已是 PASS_DELIVERED 且 sop_path 未变时跳过追加（幂等） |
| 3.4 | `dimensions` 字段口径 | 任务书模板 vs pro_qa_inspector | pro_semantic 模式的 `_qa_result.json` 也嵌入 `dimensions`（规则预检两维分数 + Pro 分），下游一个稳定 schema |
| 3.5 | 一次性修复脚本漂流 | `_trash/video-analysis-gemini-scripts-20260725/`（add_honesty.py 等 7 个） | 确认补丁已并入主脚本（mtime 显示已并入）；skill 目录加 CHANGELOG.md 记录首跑后变更，以后改脚本记一行 |
| 3.6 | 奶糕成精档案社空壳目录 | 归档根目录 | 删除或等首批分析时重建 |

---

## 验证方案（每批完成后跑）

1. **影子重跑 TOP01**（在归档目录副本上）：
   - 手动 patch analysis 后重新组包 → `_state.json` 应出现 `PROBE_REPAIRING`
   - ingest 后 → 应存在 `_pro_review_result_r1.json`
   - 旧 packet + 新 analysis ingest → 应被拒绝
2. **九宫格**：重跑 preprocessor → 9 帧填满无黑底
3. **SOP/ formula 重编译**：切频为数值、无 tuple repr、Hook 类型非空、停顿数 = ducking 事件数
4. **增量跑 TOP02**（真跑）：grounding 的 `account_baseline` 应注入 TOP01 的真实 ASL/formula，而非 unknown/0
5. 回归确认：批次 1+2 改完后 Pro 复审 TOP01 分数不应显著波动（±5 分内）

## 工作量估算

| 批次 | 改动文件 | 量级 |
|---|---|---|
| 批次 1 | pro_qa_inspector.py、scan_helper.py、ip_sop_compiler.py、preprocessor.py（警告）、任务书模板 | ~150 行 |
| 批次 2 | schema_contract.json（新）、validate_schema/unified_gate/pro_qa_inspector、synthesis_engine、ip_sop_compiler、preprocessor.py、SKILL.md | ~200 行 |
| 批次 3 | synthesis_engine、ip_sop_compiler、pro_qa_inspector、SKILL.md、CHANGELOG.md（新） | ~80 行 |
